True Love Arrives H5

Upload index.html and the full assets folder together.

Performance/cache notes:
- CSS and JavaScript are embedded in index.html, so there are no external library/font requests.
- Banner is preloaded; below-the-fold images use lazy loading.
- Source PNG/GIF assets were converted to compressed WebP, reducing initial load size significantly.
- Asset filenames are versioned and URLs include ?v=20260909-01 to reduce stale-cache issues after deployment.
- If you replace an image later, change the filename or the v= value in index.html so mobile browsers/CDNs fetch the new asset.

WhatsApp link:
+86 186 4639 0081
